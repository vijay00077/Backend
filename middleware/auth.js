// This file is no longer needed and can be deleted
// If you want to keep it for future reference, you can comment out the code
/*
const jwt = require('jsonwebtoken');

exports.protect = async (req, res, next) => {
  // Authentication middleware code commented out
  // as it's no longer being used in the application
  next();
};
*/